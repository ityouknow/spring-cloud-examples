server:
  port: 8001

spring:
  cloud:
    config:
      server:
        svn:
          uri: http://127.0.0.1/svn/repo/config-repo
          username: admin
          password: 123456
        default-label: trunk
  profiles:
    active: subversion
  application:
    name: spring-cloud-config-server